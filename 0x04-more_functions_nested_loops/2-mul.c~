#include "main.h"
#include <stdio.h>
/**
 *mul- multiplies two integers
 *@a: an integer
 @b: an integer
 *@c: character to be checked
 *Return: 1 if is digit,otherwise 0
*/
int mul(int a, int b)
{
	return (a * b);
}
